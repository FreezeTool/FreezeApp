<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--    <uses-permission-->
    <!--        android:name="android.permission.DUMP"-->
    <!--        tools:ignore="ProtectedPermissions" />-->

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_icon"

        android:supportsRtl="true"
        android:theme="@style/Theme.FreezeApp"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenLayout|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>

            <intent-filter>
                <action android:name="com.cleanmaster.hook.LAUNCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>


        <activity
            android:name=".freeze.ManagerActivity"
            android:configChanges="orientation|screenLayout|screenSize">

        </activity>

        <activity
            android:name=".battery.BatteryUsageActivity"
            android:configChanges="orientation|screenLayout|screenSize">

        </activity>

        <activity
            android:name=".CommandActivity"
            android:configChanges="orientation|screenLayout|screenSize">

        </activity>

        <activity
            android:name=".DaemonStartActivity"
            android:configChanges="orientation|screenLayout|screenSize">

        </activity>

        <activity
            android:name=".usagestats.UsageStatsActivity"
            android:configChanges="orientation|screenLayout|screenSize">

        </activity>

        <activity
            android:name=".hyper.MiMixFlipSettingActivity"
            android:configChanges="orientation|screenLayout|screenSize">

        </activity>

        <activity
            android:name=".adb.AdbPairActivity"
            android:configChanges="orientation|screenLayout|screenSize">

        </activity>

        <activity android:name=".monitor.AppMonitorActivity">

        </activity>

        <activity android:name=".appops.AppOpsActivity">

        </activity>

        <activity android:name=".appops.AppOpsDetailActivity">

        </activity>

        <service android:name=".monitor.AppMonitorService"/>

        <service
            android:name=".adb.AdbPairService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />


        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="com.john.freezeapp.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />


        <provider
            android:name=".client.ClientProvider"
            android:authorities="com.john.freezeapp.daemon"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    </application>

</manifest>